# 🤖 Telegram ИИ-бот для преподавателей центра «Логос»

Production-готовый Telegram бот для автоматизации создания отчётов преподавателей. Интегрирован с ИИ через OpenRouter API.

## 📋 Возможности

- ✅ Обработка **текстовых сообщений**
- ✅ Обработка **голосовых сообщений** с автоматической транскрипцией (Groq Whisper - БЕСПЛАТНО)
- ✅ **Память диалога** — бот помнит контекст разговора
- ✅ **Экспорт в Excel** — готовые отчёты в формате .xlsx
- ✅ **Управляющие кнопки** — очистка истории и экспорт одной кнопкой
- ✅ Интеграция с **OpenRouter API** для доступа к различным ИИ моделям
- ✅ Использование **Claude 3.5 Haiku** по умолчанию (быстрая и эффективная модель)
- ✅ Настраиваемый системный промпт из файла `prompt.md`
- ✅ Асинхронная архитектура на **aiogram 3.x**
- ✅ Production-уровень логирования и обработки ошибок

## 🛠 Технологический стек

- **Python 3.8+**
- **aiogram 3.13.1** — библиотека для работы с Telegram Bot API
- **aiohttp** — асинхронный HTTP-клиент для запросов к OpenRouter
- **python-dotenv** — управление переменными окружения
- **pydub** — обработка аудиофайлов для транскрипции
- **openpyxl** — создание Excel файлов

## 📦 Установка

### 1. Клонируйте репозиторий или скопируйте файлы проекта

```bash
cd /путь/к/проекту
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Установите ffmpeg (требуется для pydub)

**macOS:**
```bash
brew install ffmpeg
```

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

**Windows:**
Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) и добавьте в PATH.

### 5. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните необходимые значения:

```env
# Получите токен от @BotFather в Telegram
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token

# Получите API ключ на https://openrouter.ai/keys
OPENROUTER_API_KEY=ваш_openrouter_api_key

# Опционально: выберите модель (по умолчанию Claude 3.5 Haiku)
OPENROUTER_MODEL=anthropic/claude-3.5-haiku
```

### 6. Настройте системный промпт

Файл `prompt.md` уже содержит базовый промпт. Вы можете отредактировать его под свои нужды.

## 🚀 Запуск

```bash
python bot.py
```

При успешном запуске вы увидите:

```
INFO - Системный промпт успешно загружен из prompt.md
INFO - Запуск бота с моделью: anthropic/claude-3.5-haiku
INFO - Бот готов к работе!
```

## 💡 Использование

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Отправьте текстовое или голосовое сообщение
4. Получите структурированный отчёт от AI

## 🎯 Как получить необходимые ключи

### Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### OpenRouter API Key

1. Зайдите на [OpenRouter](https://openrouter.ai/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел [Keys](https://openrouter.ai/keys)
4. Создайте новый API ключ
5. Скопируйте ключ в `.env` файл

## 🔧 Доступные модели OpenRouter

Вы можете изменить модель в `.env` файле. Популярные варианты:

- `anthropic/claude-3.5-haiku` (по умолчанию) — быстрая и эффективная
- `anthropic/claude-3.5-sonnet` — более продвинутая
- `openai/gpt-4o` — GPT-4 Omni от OpenAI
- `openai/gpt-4o-mini` — компактная версия GPT-4
- `google/gemini-pro-1.5` — Gemini от Google

Полный список моделей: [OpenRouter Models](https://openrouter.ai/models)

## 📁 Структура проекта

```
ai-tg-bot/
├── bot.py              # Основной код бота
├── prompt.md           # Системный промпт для LLM
├── requirements.txt    # Зависимости Python
├── .env.example        # Шаблон переменных окружения
├── .env               # Ваши переменные окружения (не комитится)
├── .gitignore         # Игнорируемые файлы
└── README.md          # Документация
```

## 🔒 Безопасность

- ❗ **Никогда не коммитьте файл `.env`** с вашими ключами
- ❗ **Не публикуйте токены** в открытых репозиториях
- ✅ Используйте `.env.example` для документирования необходимых переменных
- ✅ Добавьте `.env` в `.gitignore` (уже добавлено)

## 🐛 Устранение неполадок

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"
Убедитесь, что создан файл `.env` и в нём указан токен.

### Ошибка: "Файл prompt.md не найден"
Проверьте, что файл `prompt.md` находится в той же директории, что и `bot.py`.

### Ошибка при обработке голосовых сообщений
Убедитесь, что установлен `ffmpeg`:
```bash
ffmpeg -version
```

### Проблемы с транскрипцией
OpenRouter использует Whisper для транскрипции. Убедитесь, что:
- Голосовое сообщение чёткое и разборчивое
- У вас достаточно кредитов на OpenRouter

## 📊 Логирование

Бот логирует все важные события:
- Загрузка системного промпта
- Получение сообщений от пользователей
- Транскрипцию голосовых сообщений
- Запросы к OpenRouter API
- Ошибки и исключения

## 🤝 Вклад

Проект готов к использованию и расширению. Основные возможности для улучшения:
- Добавление истории диалогов (с использованием БД)
- Поддержка изображений
- Кэширование ответов
- Мультиязычность интерфейса

## 📝 Лицензия

MIT License - используйте свободно в своих проектах.

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность токенов в `.env`
2. Наличие файла `prompt.md`
3. Установку всех зависимостей
4. Логи бота для диагностики

---

**Создано с использованием aiogram 3.x и OpenRouter API**
