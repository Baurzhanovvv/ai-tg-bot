# 🚀 Деплой бота на сервер

Это руководство поможет развернуть Telegram бота на удалённом сервере (VPS).

## 📋 Требования к серверу

- **ОС:** Ubuntu 20.04+ / Debian 11+ / CentOS 8+
- **RAM:** Минимум 512 MB (рекомендуется 1 GB)
- **CPU:** 1 ядро
- **Disk:** 10 GB
- **Python:** 3.8+

## 🛠 Рекомендуемые VPS провайдеры

### Бюджетные варианты:
- **DigitalOcean** — от $6/месяц (droplet)
- **Hetzner** — от €4.5/месяц
- **Vultr** — от $6/месяц
- **Linode** — от $5/месяц

### Российские провайдеры:
- **Selectel** — от 300₽/месяц
- **Timeweb** — от 200₽/месяц
- **REG.RU** — от 250₽/месяц

---

## 📦 Установка на Ubuntu/Debian сервер

### Шаг 1: Подключение к серверу

```bash
ssh root@ваш_ip_адрес
```

### Шаг 2: Обновление системы

```bash
apt update && apt upgrade -y
```

### Шаг 3: Установка необходимых пакетов

```bash
# Python и pip
apt install -y python3 python3-pip python3-venv

# ffmpeg для обработки аудио
apt install -y ffmpeg

# Git (опционально)
apt install -y git
```

### Шаг 4: Создание пользователя для бота (рекомендуется)

```bash
# Создаём пользователя
adduser botuser

# Даём права sudo (опционально)
usermod -aG sudo botuser

# Переключаемся на пользователя
su - botuser
```

### Шаг 5: Загрузка проекта на сервер

**Вариант A: Через Git (рекомендуется)**

```bash
cd ~
git clone https://ваш-репозиторий.git ai-tg-bot
cd ai-tg-bot
```

**Вариант B: Через SCP (с локального компьютера)**

```bash
# На вашем локальном компьютере
scp -r /Users/amirbaurzhanov/Desktop/projects/KWORK/ai-tg-bot botuser@ваш_ip:/home/botuser/
```

**Вариант C: Ручная загрузка через SFTP**
- Используйте FileZilla, Cyberduck или WinSCP
- Подключитесь к серверу
- Загрузите все файлы проекта

### Шаг 6: Настройка виртуального окружения

```bash
cd ~/ai-tg-bot

# Создаём виртуальное окружение
python3 -m venv venv

# Активируем
source venv/bin/activate

# Устанавливаем зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 7: Настройка .env файла

```bash
# Копируем пример
cp .env.example .env

# Редактируем (используйте nano или vim)
nano .env
```

Заполните все необходимые ключи:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
OPENROUTER_API_KEY=ваш_ключ_openrouter
OPENROUTER_MODEL=anthropic/claude-3.5-haiku
GROQ_API_KEY=ваш_ключ_groq
```

Сохраните (Ctrl+O, Enter) и выйдите (Ctrl+X).

### Шаг 8: Проверка настройки промпта

```bash
# Убедитесь что prompt.md существует
cat prompt.md

# Если нужно отредактировать
nano prompt.md
```

### Шаг 9: Тестовый запуск

```bash
# Активируйте venv если ещё не активирован
source venv/bin/activate

# Запустите бота
python3 bot.py
```

Вы должны увидеть:
```
INFO - Системный промпт успешно загружен из prompt.md
INFO - ✅ Groq API ключ обнаружен - голосовые сообщения включены
INFO - ✅ ffmpeg установлен - обработка аудио доступна
INFO - Запуск бота с моделью: anthropic/claude-3.5-haiku
INFO - Бот готов к работе!
```

Протестируйте бота в Telegram. Если всё работает — нажмите Ctrl+C для остановки.

---

## 🔄 Настройка автозапуска (systemd)

Чтобы бот запускался автоматически и работал в фоне:

### Шаг 1: Создание systemd сервиса

```bash
sudo nano /etc/systemd/system/telegram-ai-bot.service
```

Вставьте следующий конфиг (замените пути на свои):

```ini
[Unit]
Description=Telegram AI Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/ai-tg-bot
Environment="PATH=/home/botuser/ai-tg-bot/venv/bin"
ExecStart=/home/botuser/ai-tg-bot/venv/bin/python3 /home/botuser/ai-tg-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните и выйдите (Ctrl+O, Enter, Ctrl+X).

### Шаг 2: Активация сервиса

```bash
# Перезагрузить конфигурацию systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable telegram-ai-bot

# Запустить сервис
sudo systemctl start telegram-ai-bot

# Проверить статус
sudo systemctl status telegram-ai-bot
```

### Шаг 3: Полезные команды для управления

```bash
# Остановить бота
sudo systemctl stop telegram-ai-bot

# Перезапустить бота
sudo systemctl restart telegram-ai-bot

# Посмотреть логи
sudo journalctl -u telegram-ai-bot -f

# Посмотреть последние 100 строк логов
sudo journalctl -u telegram-ai-bot -n 100
```

---

## 🔐 Безопасность

### 1. Защита .env файла

```bash
# Установите правильные права доступа
chmod 600 .env

# Убедитесь что владелец — ваш пользователь
chown botuser:botuser .env
```

### 2. Настройка файрвола (UFW)

```bash
# Установить UFW
sudo apt install -y ufw

# Разрешить SSH
sudo ufw allow ssh

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

### 3. Не храните .env в Git

Убедитесь что `.env` добавлен в `.gitignore`:

```bash
echo ".env" >> .gitignore
```

---

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
sudo journalctl -u telegram-ai-bot -f
```

### Проверка использования ресурсов

```bash
# Использование CPU и RAM
htop

# Процессы Python
ps aux | grep python
```

---

## 🔄 Обновление бота

### Если используете Git:

```bash
cd ~/ai-tg-bot
git pull
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart telegram-ai-bot
```

### Если загружаете вручную:

1. Загрузите новые файлы через SCP/SFTP
2. Перезапустите сервис:
```bash
sudo systemctl restart telegram-ai-bot
```

---

## ❌ Решение проблем

### Бот не запускается

```bash
# Проверьте логи
sudo journalctl -u telegram-ai-bot -n 50

# Проверьте права на файлы
ls -la ~/ai-tg-bot/

# Проверьте что venv активен и зависимости установлены
source ~/ai-tg-bot/venv/bin/activate
pip list
```

### Ошибки с ffmpeg

```bash
# Проверьте установку
ffmpeg -version

# Переустановите если нужно
sudo apt install --reinstall ffmpeg
```

### Бот падает после запуска

```bash
# Увеличьте лимит файлов (добавьте в /etc/systemd/system/telegram-ai-bot.service)
LimitNOFILE=65536

# Перезагрузите конфиг
sudo systemctl daemon-reload
sudo systemctl restart telegram-ai-bot
```

---

## 📈 Масштабирование (опционально)

### Для высоконагруженных ботов:

1. **Используйте Redis** для кеширования
2. **Настройте nginx** как reverse proxy
3. **Используйте Docker** для изоляции
4. **Настройте мониторинг** (Prometheus + Grafana)

---

## ✅ Чек-лист деплоя

- [ ] Сервер создан и настроен
- [ ] Python 3.8+ установлен
- [ ] ffmpeg установлен
- [ ] Проект загружен на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл настроен с реальными ключами
- [ ] prompt.md настроен
- [ ] Тестовый запуск успешен
- [ ] systemd сервис создан
- [ ] Автозапуск включён
- [ ] Файрвол настроен
- [ ] Бот работает в фоне
- [ ] Логи проверены

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo journalctl -u telegram-ai-bot -n 100`
2. Убедитесь что все API ключи корректны
3. Проверьте что ffmpeg установлен
4. Проверьте права доступа к файлам

---

**Готово! Ваш бот теперь работает 24/7 на сервере!** 🎉
