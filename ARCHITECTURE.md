# 🏗️ Архитектура проекта

## Структура файлов

```
ai-tg-bot/
├── bot.py              # Основной код бота (400+ строк)
├── prompt.md           # Системный промпт для LLM
├── requirements.txt    # Python зависимости
├── .env                # Переменные окружения (НЕ коммитить!)
├── .env.example        # Шаблон для .env
├── .gitignore          # Git исключения
├── start.sh            # Скрипт быстрого запуска
├── README.md           # Полная документация
└── QUICKSTART.md       # Быстрый старт
```

## Основные компоненты bot.py

### 1. Инициализация и конфигурация
- Загрузка переменных окружения из `.env`
- Настройка логирования
- Валидация обязательных параметров

### 2. Загрузка системного промпта
```python
load_system_prompt() -> bool
```
- Читает файл `prompt.md` при старте
- Сохраняет в глобальную переменную `SYSTEM_PROMPT`
- Логирует ошибки если файл не найден

### 3. Транскрипция аудио
```python
transcribe_audio(audio_path: str) -> Optional[str]
```
- Конвертирует `.oga` файлы в `.mp3`
- Отправляет в OpenRouter Whisper API
- Возвращает распознанный текст
- Автоматически удаляет временные файлы

### 4. Обращение к LLM
```python
get_llm_response(user_message: str) -> Optional[str]
```
- Формирует запрос к OpenRouter API
- Использует системный промпт из `prompt.md`
- Добавляет сообщение пользователя
- Возвращает ответ от модели

### 5. Обработчики сообщений

#### `/start` команда
```python
@dp.message(Command("start"))
async def cmd_start(message: Message)
```
- Приветствие пользователя
- Краткая инструкция

#### Голосовые сообщения
```python
@dp.message(F.voice)
async def handle_voice(message: Message)
```
- Скачивает голосовое сообщение
- Транскрибирует через Whisper
- Отправляет текст в LLM
- Возвращает ответ пользователю

#### Текстовые сообщения
```python
@dp.message(F.text)
async def handle_text(message: Message)
```
- Принимает текст пользователя
- Отправляет в LLM
- Возвращает структурированный ответ

## Поток обработки сообщений

### Текстовое сообщение:
```
Пользователь → Telegram → Bot.py → OpenRouter API → LLM → Ответ → Пользователь
```

### Голосовое сообщение:
```
Пользователь → Telegram (Voice) → Bot.py
    ↓
Скачивание аудио (.oga)
    ↓
Конвертация в .mp3 (pydub)
    ↓
Whisper API (OpenRouter) → Текст
    ↓
OpenRouter API → LLM → Ответ → Пользователь
```

## OpenRouter API интеграция

### Endpoint для Chat Completions:
```
POST https://openrouter.ai/api/v1/chat/completions
```

### Структура запроса:
```json
{
  "model": "anthropic/claude-3.5-haiku",
  "messages": [
    {
      "role": "system",
      "content": "<содержимое prompt.md>"
    },
    {
      "role": "user",
      "content": "<текст пользователя>"
    }
  ]
}
```

### Endpoint для Whisper (транскрипция):
```
POST https://openrouter.ai/api/v1/audio/transcriptions
```

## Обработка ошибок

### Уровни защиты:
1. **Валидация при старте** - проверка токенов и промпта
2. **Try-catch блоки** - перехват всех исключений
3. **Логирование** - детальные логи для отладки
4. **Пользовательские сообщения** - понятные ошибки для юзера

### Примеры обработки:
- Отсутствие `.env` → остановка бота с ошибкой
- Отсутствие `prompt.md` → остановка бота с ошибкой
- Ошибка API → сообщение пользователю о временной проблеме
- Ошибка транскрипции → предложение отправить текст

## Безопасность

### Что защищено:
✅ `.env` файл в `.gitignore`
✅ Токены не в коде, только в переменных окружения
✅ Временные аудио файлы удаляются после обработки
✅ Нет сохранения персональных данных

### Что НЕ делается (по требованиям):
❌ Нет базы данных
❌ Нет истории переписки
❌ Нет сохранения пользовательских данных

## Production готовность

### ✅ Реализовано:
- Асинхронная архитектура (aiogram 3.x)
- Правильная обработка ошибок
- Детальное логирование
- Конфигурация через .env
- Graceful shutdown
- Типизация (type hints)

### 📋 Для полноценного продакшена добавить:
- Мониторинг (Prometheus/Grafana)
- Rate limiting
- База данных для истории (PostgreSQL)
- Docker контейнеризация
- CI/CD pipeline
- Unit тесты
- Metrics и алерты

## Зависимости

### Основные:
- `aiogram 3.13.1` - современная библиотека для Telegram Bot API
- `aiohttp 3.10.10` - асинхронные HTTP запросы
- `python-dotenv 1.0.1` - управление .env файлами
- `pydub 0.25.1` - обработка аудио

### Системные:
- `ffmpeg` - требуется для pydub (конвертация аудио)

## Расширяемость

Проект легко расширить:

### История диалогов:
```python
# Добавить Redis/PostgreSQL
# Хранить messages по chat_id
# Отправлять всю историю в LLM
```

### Поддержка изображений:
```python
@dp.message(F.photo)
async def handle_photo(message: Message):
    # Vision модели в OpenRouter
```

### Команды управления:
```python
@dp.message(Command("reset"))
async def cmd_reset(message: Message):
    # Очистить историю пользователя
```

### Webhook вместо polling:
```python
# Для production лучше использовать webhook
# Более эффективно и масштабируемо
```
